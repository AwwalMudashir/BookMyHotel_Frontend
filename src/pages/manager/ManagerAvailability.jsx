import { useEffect, useState } from 'react';
import toast from 'react-hot-toast';
import ManagerLayout from '../../components/manager/ManagerLayout';
import hotelApi from '../../api/hotelApi';
import availabilityApi from '../../api/availabilityApi';
import { useAuth } from '../../hooks/useAuth';
import { parseApiError } from '../../utils/parseApiError';

const ManagerAvailability = () => {
  const { user } = useAuth(); const hotelId = user?.managedHotel?.id;
  const [branches, setBranches] = useState([]); const [branchId, setBranchId] = useState(''); const [rooms, setRooms] = useState([]); const [roomId, setRoomId] = useState('');
  const [form, setForm] = useState({ startDate: '', endDate: '', isAvailable: true, customPrice: '', reason: '' }); const [saving, setSaving] = useState(false);
  useEffect(() => { if (!hotelId) return; hotelApi.getHotelBranches(hotelId).then((items) => { setBranches(items); if (items[0]) setBranchId(String(items[0].id)); }); }, [hotelId]);
  useEffect(() => { if (!branchId) return; hotelApi.getBranchRooms(branchId).then((items) => { setRooms(items); setRoomId(items[0] ? String(items[0].id) : ''); }); }, [branchId]);
  const submit = async (event) => { event.preventDefault(); if (!roomId || !form.startDate || !form.endDate) return toast.error('Select a room and date range.'); setSaving(true); try { await availabilityApi.setAvailability(roomId, { startDate: form.startDate, endDate: form.endDate, isAvailable: form.isAvailable, customPrice: form.customPrice === '' ? null : Number(form.customPrice), reason: form.reason.trim() || null }); toast.success('Availability and rates updated.'); } catch (err) { toast.error(parseApiError(err, 'Unable to update availability.')); } finally { setSaving(false); } };
  return <ManagerLayout><div className="mx-auto max-w-4xl"><p className="text-xs font-semibold uppercase tracking-[0.22em] text-[#0A7C6E]">Inventory control</p><h1 className="mt-2 font-[Playfair_Display] text-3xl font-semibold">Availability</h1><p className="mt-1 text-sm text-slate-500">Block dates, reopen inventory, or apply a custom nightly price.</p><form onSubmit={submit} className="mt-6 rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium text-slate-700">Branch<select value={branchId} onChange={(event) => setBranchId(event.target.value)} className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3">{branches.map((branch) => <option key={branch.id} value={branch.id}>{branch.name || branch.city}</option>)}</select></label><label className="text-sm font-medium text-slate-700">Room<select value={roomId} onChange={(event) => setRoomId(event.target.value)} className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3">{rooms.map((room) => <option key={room.id} value={room.id}>{room.roomType || room.roomTypeName} · {room.roomId || room.id}</option>)}</select></label><label className="text-sm font-medium text-slate-700">Start date<input type="date" value={form.startDate} onChange={(event) => setForm((current) => ({ ...current, startDate: event.target.value }))} className="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3" /></label><label className="text-sm font-medium text-slate-700">End date<input type="date" min={form.startDate} value={form.endDate} onChange={(event) => setForm((current) => ({ ...current, endDate: event.target.value }))} className="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3" /></label><label className="text-sm font-medium text-slate-700">Inventory status<select value={String(form.isAvailable)} onChange={(event) => setForm((current) => ({ ...current, isAvailable: event.target.value === 'true' }))} className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3"><option value="true">Available</option><option value="false">Blocked / unavailable</option></select></label><label className="text-sm font-medium text-slate-700">Custom nightly price (optional)<input type="number" min="0" step="0.01" value={form.customPrice} onChange={(event) => setForm((current) => ({ ...current, customPrice: event.target.value }))} className="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3" /></label></div><label className="mt-4 block text-sm font-medium text-slate-700">Reason (optional)<input value={form.reason} onChange={(event) => setForm((current) => ({ ...current, reason: event.target.value }))} placeholder="Maintenance, event pricing…" className="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3" /></label><button type="submit" disabled={saving || !roomId} className="mt-6 w-full rounded-2xl bg-[#0A7C6E] px-4 py-3 text-sm font-semibold text-white disabled:opacity-50">{saving ? 'Saving…' : 'Apply changes'}</button></form></div></ManagerLayout>;
};
export default ManagerAvailability;
