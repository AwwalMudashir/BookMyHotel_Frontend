// Purpose: Booking status badge for confirmed, pending, and cancelled states.
const Badge = ({ children }) => <span className="rounded-full bg-slate-200 px-2 py-1 text-sm">{children}</span>;
export default Badge;
